/* ============================================================
 *  fixed_point.c — currently empty.
 *
 *  All operations are `static inline` in the header. This .c file
 *  exists so the build system has something to compile per module,
 *  for consistency with the other modules and to give us a place
 *  to put any non-inlinable helpers later (e.g., lookup tables for
 *  trig functions when we add them).
 *
 *  Public domain (CC0). No warranty.
 * ============================================================ */

#include "math/fixed_point.h"

/* Intentionally empty. */
